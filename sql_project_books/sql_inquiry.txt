Задание 1.2

SELECT  Название
FROM Книги JOIN Экземпляры книг on Книги.ID книги = Экземпляры книг.ID экземпляра
JOIN Выдача ON Выдача.ID экземпляра=Экземпляры книг.ID книги
WHERE название='Война и мир' AND Дата возврата is not NULL 

SELECT  название
FROM Книги JOIN Экземпляры_книг on АЙдикниги = Айдижкз
JOIN Выдача ON АЙдиэк=Айдикниги
WHERE название='Война и мир' AND датавозврата is not NULL 